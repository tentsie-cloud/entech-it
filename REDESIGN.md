# ENTECH redesign preview

This branch deploys the reviewed static website. Vercel extracts entech-redesign.tar.gz and serves only dist/. The existing Next.js source is retained but is not used by this branch deployment. Main remains unchanged.

To update: regenerate the static site locally, package dist/ into entech-redesign.tar.gz, and commit it to this branch. Booking requests open WhatsApp; callbacks use Calendly. Google reviews currently link to Google rather than loading a live feed. Pages remain noindex during review.
